name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Run tests
        run: |
          echo "🧪 CI Workflow"
          echo ""
          echo "This is a placeholder CI workflow."
          echo "Agents will update this file with project-specific test commands."
          echo ""
          echo "✅ CI completed (no tests configured yet)"
